package io.eventx.http;

public class HttpProjectionPoller {
  // todo implement a http projection. each one must have a name...and they should be able to :
  // 1) able to automatically manage offset
  // 2) able to reset their own offset
  // 3) able to set their query filter
  // 4) able to set their batch size
}
